<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>GSOB Transit - Ticket Success</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #e0f2fe;
            color: #111827;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .card {
            background: rgba(255, 255, 255, 0.5);
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }
        .success-message {
            color: #10b981;
            font-weight: bold;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        .btn {
            background: linear-gradient(135deg, #1e40af, #FFD700);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            cursor: pointer;
            transition: transform 0.2s;
            text-decoration: none;
        }
        .btn:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="card">
        <h1 class="success-message">Booking Successful!</h1>
        <p class="mb-4">Thank you, <strong><%= fullName || 'User' %></strong>, for booking your ticket.</p>
        <% 
            // Split the destination to separate location and price
            const destParts = destination ? destination.split(' ') : [];
            const pricePart = destParts.pop();
            const locationPart = destParts.join(' ');
        %>
        <p class="mb-2">Destination: <strong><%= locationPart || 'Unknown' %></strong></p>
        <p class="mb-2">Price: <strong><%= pricePart || 'Unknown' %></strong></p>
        <p class="mb-4">MTN Phone Number: <strong><%= phoneNumber || 'Not Provided' %></strong></p>
        <p class="text-sm text-gray-600">
            Booked on: <strong>
            <%= new Date().toLocaleString('en-US', {
                    weekday: 'long', year: 'numeric', month: 'long', day: 'numeric',
                    hour: 'numeric', minute: 'numeric', hour12: true, timeZone: 'Africa/Kigali'
                }) %> CAT</strong>
        </p>
        <a href="/" class="btn mt-4 inline-block">Back to Home</a>
    </div>
</body>
</html>

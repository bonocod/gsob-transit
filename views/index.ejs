<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GSOB Transit - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: { primary: "#2563EB", gold: "#FFD700" },
        },
      },
    };
  </script>
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
  />
</head>
<body class="bg-gray-50 font-sans">
  <header class="bg-white shadow-md sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <a href="/" class="text-2xl font-bold text-primary">GSOB Transit</a>
        <nav aria-label="Main navigation" class="flex items-center">
          <% if (currentStudent) { %>
            <button
              class="sm:hidden text-gray-800 focus:outline-none"
              onclick="document.getElementById('mobile-menu').classList.toggle('hidden')"
            >
              <i class="fas fa-bars text-2xl"></i>
            </button>
            <ul
              id="mobile-menu"
              class="hidden sm:flex sm:items-center sm:space-x-4 flex-col sm:flex-row absolute sm:static top-16 left-0 w-full sm:w-auto bg-white sm:bg-transparent shadow-md sm:shadow-none p-4 sm:p-0 space-y-4 sm:space-y-0"
            >
              <li class="text-gray-800">
                <span>Welcome, <%= currentStudent.name %></span>
              </li>
              <li>
                <a
                  href="/booking"
                  class="text-gray-800 hover:text-primary transition-colors duration-200"
                >Book Ticket</a>
              </li>
              <li>
                <a
                  href="/auth/logout"
                  class="text-gray-800 hover:text-primary transition-colors duration-200"
                >Logout</a>
              </li>
            </ul>
          <% } %>
        </nav>
      </div>
    </div>
  </header>

  <main
    class="min-h-screen flex items-center justify-center px-4 py-12 bg-gradient-to-b from-gray-50 to-gray-100"
  >
    <div class="text-center max-w-3xl mx-auto">
      <h1 class="text-4xl sm:text-5xl font-bold mb-4 text-gray-900">
        GSOB <span class="text-primary">TRANSIT</span>
      </h1>
      <p class="text-lg sm:text-xl text-gray-700 mb-8">
        Your Safe Learning Journey Starts Here
      </p>
      <% if (currentStudent) { %>
        <p class="text-base sm:text-lg text-gray-600 mb-6">
          Book your ticket to your next destination with ease.
        </p>
        <a
          href="/booking"
          class="inline-block bg-primary text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors duration-200 text-lg font-semibold"
        >
          Book Now
        </a>
      <% } else { %>
        <p class="text-base sm:text-lg text-gray-600 mb-6">
          Enter your student code to get started.
        </p>
        <div class="max-w-md mx-auto bg-white p-6 sm:p-8 rounded-lg shadow-lg">
          <h2 class="text-2xl sm:text-3xl font-bold text-center text-gray-800 mb-6">Enter Your Code</h2>
          <% if (errorMessage) { %>
            <div role="alert" class="bg-red-100 text-red-800 px-4 py-3 mb-6 rounded-lg border border-red-300">
              <%= errorMessage %>
            </div>
          <% } %>
          <form action="/student/verify-code" method="POST" class="space-y-6" aria-label="Code Entry Form">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div>
              <label for="code" class="block text-sm font-medium text-gray-700">Student Code</label>
              <input
                type="text"
                name="code"
                id="code"
                required
                
                
                class="mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary text-base"
                aria-required="true"
                placeholder="e.g., 1234"
              >
            </div>
            <button
              type="submit"
              class="w-full bg-primary text-white py-3 rounded-lg hover:bg-blue-700 transition-colors duration-200 text-lg font-semibold"
            >
              Submit
            </button>
          </form>
        </div>
      <% } %>
    </div>
  </main>

  <footer class="bg-gray-800 text-white py-12">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-8">
        <div>
          <h3 class="text-xl font-bold mb-4">About GSOB Transit</h3>
          <p class="text-gray-300 text-sm">
            GSOB Transit is your gateway to seamless travel for learning
            experiences. Book your ticket and connect with your educational
            journey.
          </p>
        </div>
        <div>
          <h3 class="text-xl font-bold mb-4">Connect</h3>
          <div class="flex space-x-4">
            <a
              href="#"
              class="text-gray-300 hover:text-primary transition-colors duration-200"
            ><i class="fab fa-twitter text-2xl"></i></a>
            <a
              href="#"
              class="text-gray-300 hover:text-primary transition-colors duration-200"
            ><i class="fab fa-instagram text-2xl"></i></a>
            <a
              href="mailto:info@gsob.com"
              class="text-gray-300 hover:text-primary transition-colors duration-200"
            ><i class="fas fa-envelope text-2xl"></i></a>
            <a
              href="#"
              class="text-gray-300 hover:text-primary transition-colors duration-200"
            ><i class="fas fa-users text-2xl"></i></a>
          </div>
        </div>
      </div>
      <div class="mt-8 text-center">
        <p class="text-gray-300 text-sm">
          Developed by the GSOB Team • © 2025 GSOB Transit. All rights reserved.
        </p>
      </div>
    </div>
  </footer>
</body>
</html>
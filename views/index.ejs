<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GSOB Transit - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: { primary: '#2563EB', gold: '#FFD700' }
        }
      }
    }
  </script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />
</head>
<body class="bg-gray-100">
  <header class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <a href="/" class="text-xl font-bold text-primary">GSOB Transit</a>
        <nav aria-label="Main navigation">
          <ul class="flex items-center space-x-4">
            <% if (currentUser) { %>
              <li><span class="text-gray-800">Welcome, <%= currentUser.name %></span></li>
              <% if (currentUser.role === 'admin') { %>
                <li><a href="/admin/dashboard" class="text-gray-800 hover:text-primary transition-colors">Admin Dashboard</a></li>
              <% } else { %>
                <li><a href="/booking" class="text-gray-800 hover:text-primary transition-colors">Book Ticket</a></li>
              <% } %>
              <li><a href="/auth/logout" class="text-gray-800 hover:text-primary transition-colors">Logout</a></li>
            <% } else { %>
              <li><a href="/auth/login" class="text-gray-800 hover:text-primary transition-colors">Login</a></li>
              <li><a href="/auth/register" class="text-gray-800 hover:text-primary transition-colors">Register</a></li>
            <% } %>
          </ul>
        </nav>
      </div>
    </div>
  </header>

  <div class="min-h-screen flex flex-col items-center justify-center px-4 py-12">
    <div class="text-center mb-12">
      <h1 class="text-5xl md:text-6xl font-bold mb-4">GSOB <span class="text-primary">TRANSIT</span></h1>
      <p class="text-xl text-gray-700">Your Safe Learning Journey Starts Here</p>
    </div>

    <div class="max-w-4xl mx-auto text-center">
      <% if (currentUser) { %>
        <p class="text-lg text-gray-800 mb-6">
          <% if (currentUser.role === 'admin') { %>
            Manage student bookings, view summaries, and track payments from the admin dashboard.
          <% } else { %>
            Book your ticket to your next learning destination with ease.
          <% } %>
        </p>
        <a href="<%= currentUser.role === 'admin' ? '/admin/dashboard' : '/booking' %>" class="inline-block bg-primary text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition">
          <%= currentUser.role === 'admin' ? 'Go to Dashboard' : 'Book Now' %>
        </a>
      <% } else { %>
        <p class="text-lg text-gray-800 mb-6">Join GSOB Transit to book your learning journey today.</p>
        <div class="flex justify-center space-x-4">
          <a href="/auth/register" class="inline-block bg-primary text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition">Register</a>
          <a href="/auth/login" class="inline-block bg-gray-500 text-white px-6 py-3 rounded-lg hover:bg-gray-600 transition">Login</a>
        </div>
      <% } %>
    </div>
  </div>

  <footer class="bg-gray-800 text-white py-12">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div>
          <h3 class="text-2xl font-bold mb-4">About GSOB Transit</h3>
          <p class="text-gray-300">GSOB Transit is your gateway to transformative learning experiences. Book your ticket to world-class events and connect with global leaders.</p>
        </div>
        <div>
          <h3 class="text-2xl font-bold mb-4">Connect</h3>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-300 hover:text-primary"><i class="fab fa-twitter text-2xl"></i></a>
            <a href="#" class="text-gray-300 hover:text-primary"><i class="fab fa-instagram text-2xl"></i></a>
            <a href="mailto:info@gsob.com" class="text-gray-300 hover:text-primary"><i class="fas fa-envelope text-2xl"></i></a>
            <a href="#" class="text-gray-300 hover:text-primary"><i class="fas fa-users text-2xl"></i></a>
          </div>
        </div>
      </div>
      <div class="mt-8 text-center">
        <p class="text-gray-300">Developed by the GSOB Team • © 2025 GSOB Transit. All rights reserved.</p>
      </div>
    </div>
  </footer>
</body>
</html>
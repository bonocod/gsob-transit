<!DOCTYPE html>
<html>
<head>
  <title>Admin Summary - GSOB Transit</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2>Booking Summary by Promotion & Destination</h2>

    <table class="table table-bordered table-striped mt-4">
      <thead class="table-dark">
        <tr>
          <th>#</th>
          <th>Promotion</th>
          <th>Destination</th>
          <th>Students</th>
          <th>Total Paid (RWF)</th>
        </tr>
      </thead>
      <tbody>
        <% destinationSummary.forEach((item, i) => { %>
          <tr class="<%= item.promotion === 'All' ? 'table-info' : '' %>">
            <td><%= i + 1 %></td>
            <td><%= item.promotion %></td>
            <td><%= item.destination || '—' %></td>
            <td><%= item.numberOfStudents %></td>
            <td><%= item.totalPaid.toLocaleString() %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <div class="mb-3">
      <a href="/admin/export/summary" class="btn btn-success">Download CSV</a>
    </div>


    <h2 class="mt-5">Manage Students by Promotion</h2>
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% ['S1','S2','S3','S4','S5','S6'].forEach(promo => { %>
        <div class="col">
          <div class="card shadow-sm text-center h-100">
            <div class="card-body d-flex flex-column justify-content-center">
              <h5 class="card-title">Promotion <%= promo %></h5>
              <a href="/admin/manage/<%= promo %>" class="btn btn-primary mt-3">Manage</a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>


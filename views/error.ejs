<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - GSOB Transit</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: { primary: '#2563EB', gold: '#FFD700' }
        }
      }
    }
  </script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />
</head>
<body class="bg-gradient-to-br from-gray-50 to-gray-100 flex items-center justify-center min-h-screen">
  <div class="container mx-auto mt-10 px-4 sm:px-6 lg:px-8 flex flex-col items-center justify-center min-h-screen">
    <i class="fas fa-exclamation-circle text-6xl sm:text-7xl text-red-500 mb-4"></i>
    <h1 class="text-4xl sm:text-5xl font-bold text-gray-800 mb-4">
      <%= typeof error !== 'undefined' && error && error.status ? error.status : 500 %>
    </h1>
    <div class="bg-red-100 text-red-800 px-4 py-3 mb-6 rounded-lg border border-red-300 max-w-lg text-center">
      <%= typeof error !== 'undefined' && error && error.message ? error.message : (message || 'Something went wrong') %>
    </div>
    <a href="/" class="inline-block bg-primary text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors duration-200 text-lg font-semibold">
      Back to Home
    </a>
  </div>
</body>
</html>